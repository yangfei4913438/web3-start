import React from 'react';
import { Button } from '@material-tailwind/react';

// 签名按钮
const Signature = () => {
  return (
    <div>
      <Button className="w-32">签名</Button>
    </div>
  );
};

export default Signature;
