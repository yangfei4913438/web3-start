import React from 'react';
import { Button } from '@material-tailwind/react';

const Contract = () => {
  return (
    <div>
      <Button className="w-32">读取合约数据</Button>
    </div>
  );
};

export default Contract;
